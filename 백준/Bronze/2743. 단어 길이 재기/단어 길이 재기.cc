#include <bits/stdc++.h>
using namespace std;  

char c[104];
int cnt;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    scanf("%s", c);
    printf("%ld\n", strlen(c));

    return 0;
}
