#include <bits/stdc++.h>
using namespace std;  

int N;
char ch;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    scanf("%c", &ch);
    printf("%d\n", ch);

    return 0;
}
