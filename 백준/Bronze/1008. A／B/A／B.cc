#include <bits/stdc++.h>
using namespace std;  

double a, b;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    scanf("%lf %lf", &a, &b);
    printf("%.10lf\n", a/b);

    return 0;
}
